document.addEventListener(`DOMContentLoaded`, function () {

  const mainScreen = document.querySelector(`#root`);

  const screenAppend = (screen) => {
    mainScreen.innerHTML = screen;
  };

  import {moduleOneElement} from `../src/index2.js`
});
